{
    "buildCommand": "npm run build",
    "outputDirectory": ".next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/pinnacle-nepal/:path*",
            "destination": "/:path*"
        }
    ]
}